<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/Imagenparatal"
        android:layout_width="150dp"
        android:layout_height="100dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/Titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/Imagenparatal"
        tools:text="hola mundo"
        />

    <TextView
        android:id="@+id/Elnoseporque"
        android:layout_width="259dp"
        android:layout_height="70dp"
        app:layout_constraintStart_toEndOf="@+id/Imagenparatal"
        app:layout_constraintTop_toBottomOf="@+id/Titulo"
        tools:text="No se porque, no quieres darte la oportunidad, haces que me motive y me provocas sin necesidad, cuando estamos solos" />

</androidx.constraintlayout.widget.ConstraintLayout>